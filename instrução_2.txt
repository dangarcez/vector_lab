Quero agora que crie um arquivo python que vai gerar um arquivo excell com algumas analises básicas relacionadas aos arquivos em /logs. Primeiro, saiba que deve ignorar arquivos com final numerado (.1,.2,.3. ...) para essa análise. Umas dás analises é, quero uma lista que mostre os headers de cada arquivo, então o nome do arquivo vai ser o "indice", e em cada linha vai ter esse indice e ao lado o nome de cada header daquele arquivo (cada header em uma coluna). Outra análise que quero que faça é, quantas vezes um header aparece em todos os arquivos, deve ser algo assim: <nome_header>;<quantidade_aparições>/<quantidade_total_de_arquivos, essa listagem deve estar em ordem de numero de aparições (ordem decrescente). A ultima analise que quero que apareça nesse excel é mais simples, quero uma informação da quantidade de headers únicos somando todos esses arquivos.